services:
  - type: web
    name: dafa-erp-frontend
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: BACKEND_URL
        sync: false
      - key: NODE_ENV
        value: production
